<div class="space-y-6 text-left">
  
  <!-- Page Header -->
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 pb-6 border-b border-slate-900">
    <div>
      <h1 class="text-2xl font-mono font-black text-white uppercase tracking-tight flex items-center gap-2">
        <i data-lucide="star" class="w-6 h-6 text-yellow-400 fill-yellow-400"></i>
        <span>Quản Lý Đánh Giá Từ Khách Hàng</span>
      </h1>
      <p class="text-xs text-brand-muted mt-1">Quản lý và kiểm duyệt các đánh giá số sao, nhận xét trải nghiệm xem phòng trọ</p>
    </div>
  </div>

  <!-- Reviews Table Section -->
  <div class="glass-card rounded-2xl border border-white/5 overflow-hidden shadow-2xl">
    <div class="overflow-x-auto">
      <table class="w-full text-left text-xs text-slate-300">
        <thead class="bg-slate-950/80 text-brand-muted uppercase text-[10px] tracking-wider font-mono border-b border-slate-900">
          <tr>
            <th class="p-4">Khách hàng</th>
            <th class="p-4">Số sao</th>
            <th class="p-4">Phòng trọ</th>
            <th class="p-4">Nội dung nhận xét</th>
            <th class="p-4">Ngày gửi</th>
            <th class="p-4">Trạng thái</th>
          </tr>
        </thead>

        <tbody class="divide-y divide-slate-900/60">
          <% if (data && data.length > 0) { %>
            <% data.forEach(r => { 
              const room = rooms.find(item => item.ma_phong === r.ma_phong) || {};
            %>
              <tr class="hover:bg-white/[0.02] transition-colors">
                <td class="p-4">
                  <strong class="text-white block font-bold"><%= r.ho_ten %></strong>
                  <span class="text-[10px] text-brand-muted"><%= r.so_dien_thoai || 'Không có SĐT' %></span>
                </td>

                <td class="p-4">
                  <div class="flex items-center gap-1 text-yellow-400 font-bold">
                    <span><%= r.so_sao || 5 %></span>
                    <i data-lucide="star" class="w-3.5 h-3.5 fill-yellow-400"></i>
                  </div>
                </td>

                <td class="p-4 max-w-xs">
                  <a href="/room-details/<%= r.ma_phong %>" target="_blank" class="font-bold text-brand-accent hover:underline line-clamp-1">
                    <%= room.ten_phong || r.ma_phong %>
                  </a>
                </td>

                <td class="p-4 max-w-sm">
                  <p class="text-slate-300 leading-relaxed line-clamp-2"><%= r.noi_dung %></p>
                </td>

                <td class="p-4 font-mono text-slate-400">
                  <%= new Date(r.createdAt || Date.now()).toLocaleDateString('vi-VN') %>
                </td>

                <td class="p-4">
                  <span class="px-2.5 py-1 bg-green-500/10 text-green-400 border border-green-500/20 rounded-full text-[10px] font-bold">Đã duyệt</span>
                </td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td colspan="6" class="p-8 text-center text-brand-muted">Chưa có đánh giá nào từ khách hàng.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>

</div>
